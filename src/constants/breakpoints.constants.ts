// ==== BREAKPOINTS PX

// XS Screens: Mobile devices, Phones
const MIN_SCREEN_WIDTH_XS = 320;
const MAX_SCREEN_WIDTH_XS = 480;

// Small Screens: iPads, Tablets
const MIN_SCREEN_WIDTH_SM = 481;
const MAX_SCREEN_WIDTH_SM = 768;

// Medium Screens: Small Screens, Laptops
const MIN_SCREEN_WIDTH_MD = 769;
const MAX_SCREEN_WIDTH_MD = 1024;

// Large Screens: Desktops, large screens
const MIN_SCREEN_WIDTH_LG = 1025;
const MAX_SCREEN_WIDTH_LG = 1200;

// XL Screens: Extra large screens, TV
const MIN_SCREEN_WIDTH_XL = 1201;
const MAX_SCREEN_WIDTH_XL = 1440;

export {
  MIN_SCREEN_WIDTH_SM,
  MAX_SCREEN_WIDTH_SM,
  MIN_SCREEN_WIDTH_MD,
  MAX_SCREEN_WIDTH_MD,
  MIN_SCREEN_WIDTH_LG,
  MAX_SCREEN_WIDTH_LG,
  MIN_SCREEN_WIDTH_XL,
  MAX_SCREEN_WIDTH_XL,
};
